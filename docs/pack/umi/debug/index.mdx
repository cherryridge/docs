import { UmiFeature } from "../../../../src/components/UMI";

# Debug

## Logging

### Intrinsic Console

<UmiFeature>
    <>You can't log to console in JSON API. There is no console.</>
    <>You can use the original <code>console.log</code> etc. to log messages to the standard CherryGrove log.</>
    <>
        Use <code>console.log</code> etc. to log messages to the standard CherryGrove log:
        ```cpp
        console.log()
        ```
    </>
</UmiFeature>